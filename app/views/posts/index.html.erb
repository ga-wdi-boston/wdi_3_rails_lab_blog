  <h2>Recent Posts</h2>

<ul>
  <% @posts.each do |post| %>
    <li><%= link_to post.post_title, post %> by <%= post.post_user %> | <%=  post.created_at.to_formatted_s(:long_ordinal) %> | <%= post.comments.count %> comments </li>
  <% end %>
</ul>

<%= link_to 'new post', new_post_path %>
